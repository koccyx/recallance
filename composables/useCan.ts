import { useProductsStrore } from "@/stores/products";

export const useCan = (productId: string) => {
	const userProducts = useProductsStrore().products;
	
	return () => {
		const can = ref
		
		
	}
}